# .lychee.toml - Configuration for lychee link checker
# Used by both CI and local runs:
#   lychee ./*.html ./breaches/*.html ./meetings/*.html ./portfolio/*.html ./homelab/*.html

# Exclude patterns that block automated checks
exclude = [
  "linkedin\\.com",                # LinkedIn blocks bots with 999 status
  "web\\.archive\\.org/web/\\d+",  # Archive.org rate-limits aggressively
  "localhost",
  "127\\.0\\.0\\.1",
  "blog\\.appsecco\\.com",        # Blocks bots (403)
  "bleepingcomputer\\.com",        # Blocks bots (403)
  "community\\.sap\\.com",        # Blocks bots (403)
  "darkreading\\.com",             # Blocks bots (403)
  "dl\\.acm\\.org",               # Blocks bots (403)
  "fiverr\\.com",                  # Blocks bots (403)
  "glassdoor\\.com",               # Blocks bots (403)
  "imdb\\.com",                    # Blocks bots (403)
  "indeed\\.com",                  # Blocks bots (403)
  "instagram\\.com",               # Blocks bots (403)
  "mdpi\\.com",                    # Blocks bots (403)
  "meco\\.org",                    # Blocks bots (403)
  "medium\\.com",                  # Blocks bots (403)
  "nexos\\.ai",                    # Blocks bots (403)
  "nytimes\\.com",                 # Blocks bots (403)
  "openai\\.com",                  # Blocks bots (403)
  "help\\.otter\\.ai",            # Blocks bots (403)
  "reddit\\.com",                  # Blocks bots (403)
  "rsaconference\\.com",           # Blocks bots (403)
  "sec-consult\\.com",             # Aggressive rate limiting
  "securitytrails\\.com",          # Blocks bots (403)
  "shodan\\.io",                   # Blocks bots (403)
  "substack\\.com",                # Blocks bots (403)
  "resilientcyber\\.io",           # Substack custom domain - blocks bots (403)
  "training\\.cloudsecurityalliance\\.org",  # Blocks bots (403)
  "uber\\.com",                    # Blocks bots (406)
  "udemy\\.com",                   # Blocks bots (403)
  "upwork\\.com",                  # Blocks bots (403)
  "washingtontimes\\.com",         # Blocks bots (403)
  "zoomeye\\.org",                 # Blocks bots (521)
  "blackhat\\.com",                # Blocks bots (403)
  "euvd\\.enisa\\.europa\\.eu",   # Unreliable (502)
  "gitconnected\\.com",            # SSL certificate issues
  "jobs\\.sap\\.com",              # Timeout-prone (behind WAF)
  "programs\\.com",                # Timeout-prone
  "trustoncloud\\.com",            # Timeout-prone
  "careersinaudit\\.com",          # Bot-verification redirect (302)
  "blog\\.lastpass\\.com",         # Cloudflare bot-block (403)
  "news\\.sky\\.com",              # Bot-block (403)
  "grep\\.app",                    # Rate-limits bots (429)
  "www\\.sophos\\.com",            # WAF timeouts for automated clients
  "www\\.nsa\\.gov",               # Blocks bots (403)
  "www\\.cyber\\.gov\\.au",        # WAF/geo timeouts for automated clients
  # --- Added from broken-link triage: live hosts that block CI/automated clients ---
  "news\\.ycombinator\\.com",          # Rate-limits bots (429)
  "openlibrary\\.org",                 # Rate-limits bots (429)
  "support\\.lastpass\\.com",          # Cloudflare bot-block / SSL to bots
  "tcm-sec\\.com",                     # Blocks bots (403)
  "www\\.matanosecurity\\.com",        # Rate-limits bots (429)
  "www\\.mend\\.io",                   # Blocks bots (403)
  "www\\.technology\\.org",            # Blocks bots (403)
  "blog\\.phylum\\.io",                # Live (Veracode) but blocks bots
  "cisoseries\\.com",                  # Blocks bots (403)
  "devici\\.com",                      # Blocks bots (403)
  "docs-prv\\.pcisecuritystandards\\.org", # Blocks bots (403)
  "docs\\.openshift\\.com",            # Blocks bots (403)
  "docs\\.redhat\\.com",               # Blocks bots (403)
  "dodcio\\.defense\\.gov",            # Blocks bots (403)
  "drata\\.com",                       # Blocks bots (403)
  "flaws\\.cloud",                     # Live AWS CTF, HTTP-only, blocks bots
  "flaws2\\.cloud",                    # Live AWS CTF, HTTP-only, blocks bots
  "media\\.defense\\.gov",             # Blocks bots (403)
  "nonamesecurity\\.com",              # Blocks bots (403)
  "www\\.akamai\\.com",                # Blocks bots (403)
  "www\\.bls\\.gov",                   # Blocks bots (403)
  "www\\.cisco\\.com",                 # Blocks bots (403)
  "www\\.gartner\\.com",               # Blocks bots (403)
  "www\\.hashicorp\\.com",             # Rate-limits bots (429)
  "www\\.hhs\\.gov",                   # Blocks bots (403)
  "www\\.kroll\\.com",                 # Blocks bots (403)
  "www\\.n-able\\.com",                # Blocks bots (403)
  "www\\.npmjs\\.com",                 # Blocks bots (403)
  "www\\.opentext\\.com",              # No response to bots (444)
  "www\\.pentest-standard\\.org",      # Live PTES wiki, HTTP-only, blocks bots
  "www\\.repvue\\.com",                # Rate-limits bots (429)
  "www\\.robustintelligence\\.com",    # Live (now Cisco) but blocks bots
  "www\\.rubrik\\.com",                # Blocks bots (403)
  "www\\.secureworks\\.com",           # Redirect loop to bots
  "www\\.servicenow\\.com",            # Live but blocks bots
  "www\\.sheltered-harbor\\.org",      # Live but blocks bots
  "www\\.shmoocon\\.org",              # 500 to automated clients
  "www\\.trellix\\.com",               # Live but blocks bots
  "www\\.varmour\\.com",               # Cloudflare 525 to bots
  "www\\.visualcv\\.com",              # Rate-limits bots (429)
  "www\\.workatastartup\\.com",        # Blocks bots (406)
  "www\\.cpomagazine\\.com",           # Blocks bots (403)
  "www\\.3cx\\.com",                  # Blocks bots (403)
  "www\\.beyondtrust\\.com",           # Blocks bots (403)
  "cloud\\.google\\.com/architecture/framework/security/incident-response",  # Works in browser; redirect false-404
  "github\\.com/CloudSecurityOfficeHours/csoh\\.org/security/dependabot",     # Our repo admin-only tab; 404 to anon

  # --- Not links: strings inside the published Terraform (infra/terraform/**.tf) ---
  #
  # The .tf files are crawled too (they are published, and 65% of their lines
  # are teaching prose). But HCL is full of URL-SHAPED STRINGS that are
  # identifiers or examples, not destinations, and a crawler cannot tell the
  # difference. Every pattern below is anchored to the exact string so it
  # suppresses only that, and never weakens checking of a real link elsewhere
  # on the site - note each one ends at the host root (`/?$`), so the same
  # host WITH a path is still checked normally.
  #
  # Adding a new CSP host or federation issuer to a .tf file will surface here
  # as a fresh "404" on its bare root. That is expected; anchor it and add it.
  "^https://token\\.actions\\.githubusercontent\\.com/?$",  # OIDC issuer ID, not a page (root 404s)
  "^https://(i\\.ytimg\\.com|img\\.youtube\\.com)/?$",      # CSP frame/img allowlist hosts, bare roots
  "^https://(www|mta-sts)/?$",                              # Fragments of quoted Cloudflare rule expressions
  "^https://csoh-site-xxxx\\.run\\.app",                    # Placeholder Cloud Run URL in an example
  "^https://d111\\.cloudfront\\.net",                       # AWS's own docs-example distribution hostname
]

# Accept common redirect status codes
accept = [200, 202, 204, 301, 302, 308]

# Resolve root-relative links (e.g. /favicon.png) against the repo root
root_dir = "."

# Reasonable timeouts for external sites
timeout = 30
max_retries = 3
max_concurrency = 10

# Mail addresses are excluded by default in lychee v0.21+.
# To opt in, set include_mail = true.
